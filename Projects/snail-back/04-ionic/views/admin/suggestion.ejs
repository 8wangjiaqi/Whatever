<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>蜗牛旅行</title>
    <style media="screen">
    	.a{
			float: left;
			height: 650px;
			width: 17%;
      height: 100%;
			background-color: #207B94;
			position:fixed;
			left:10px;

		}
		.a a{
			text-decoration: none;
			display: block;
			color: white;
			height: 50px;
			line-height: 50px;
			font-size: 18px;
			padding-left: 45px;
			border-bottom: 1px dashed ghostwhite;
			
		}
		.a a:hover { 
			color:orange; 
			text-decoration: underline;
			font-size: 20px;
			font-weight: bold;
		} 
		.snail{
			width: 100%;
			text-align: center;
			margin-bottom: 30px;
		}
		.snail img{
			width: 50%;
		}
		.content-wrap{
			float: right;
			width: 82%;
			height: 650px;
		}
      .shadow{
          position:fixed;
          width:100%;
          height:100%;
          background:#000;
          opacity:0.3;
          /* filter:alpha(opacity:30);
          left:0;
          top:0; */
      }
      .mod_dialog{
        width:500px;
        height:400px;
        border:1px solid black;
        background:#fff;
        position:fixed;
        left:50%;
        top:20%;
        margin-left:-150px;
        
      }
      a{
        text-decoration: none;
        
      }
      .article{
        overflow: hidden!important;
        display: -webkit-box;
        -webkit-box-orient:vertical;
        -webkit-line-clamp:3!important;
      }
      td{
        word-break:keep-all;/* 不换行 */
        white-space:nowrap;/* 不换行 */
        overflow:hidden;/* 内容超出宽度时隐藏超出部分的内容 */
        text-overflow:ellipsis;/* 当对象内文本溢出时显示省略标记(...) ；需与overflow:hidden;一起使用。*/
        height: 40px;
      }
      table{
        table-layout:fixed;
        text-align: center;
      }
      .title{
        border:0;
        background-color: #207B94;
        height: 50px;
      }
    </style>
    </head>
<body  style="background-image: url(/upload/d1.png);">
       <div class="page">
        <div class="a">
          <div class="snail">
            <img src="/upload/snail.png"/>
          </div>
          <% include components/left-inc.ejs %>
        </div>
		
        <div class="content-wrap">
          	<% include components/admin.ejs %>
          <table border="1" width="100%" cellspacing="0" cellpadding="0" style="margin-top:-60px;">
            <thead>
              <tr class="title">
                <th style="width:5%">           ID       </th>
                <th style="width:8%">   用户名   </th>
                <th style="width:20%;"> 意见   </th>
                <th style="width:12%">  电话    </th>
              </tr>
            </thead>

            
          <tbody>
              <% for(var i=0;i<banners.length;i++){ %>
              <tr>
                <td><%= banners[i].ID%></td>
                <td><%= banners[i].username%></td>
                <td><%= banners[i].say%></td>
                <td><%= banners[i].phone%></td>
                <!-- <td>
                     <a href="?act=del&id=<%= banners[i].ID%>" onclick="return confirm('你真的要删除这条数据吗?')">删除</a>
                </td>-->
              </tr>
              <% }%>
          </tbody>
          
          </table>
        </div>
      </div>
  </body>
</html>


